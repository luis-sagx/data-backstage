# ${{ values.name }}

${{ values.description }}

## TechDocs

This repository contains TechDocs content under the `docs/` folder. When registered in Backstage, TechDocs will render the markdown files from that directory.

## What this skeleton provides

- `catalog-info.yaml` (templated)
- `docs/overview.md` and `docs/usage.md` (from template)
